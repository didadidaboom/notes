# examine the desired directiory by running:
```
du -sh /<directory>/*

df -h
```
# 不挂断启动
```
nohup <programe-command>
```

# find the latest files by modified date
```
find /path -printf '%T+ %p\n' | sort -r | head
```

#check total memeory used in current directory
du -ch ./* | grep total

# list all subdirectories size
du -shc ./*

