+ create virtual env
  ```
  # virtualenv
  virtualenv -p /usr/bin/python3 reco_sys
  ```
+ run virtualenv
 ```
 # run virtualenv
  source reco_sys/bin/activate
 ```

python3 -m venv name_env
source name_env/bin/activate
deactivate

